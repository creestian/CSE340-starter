// File: views/account/archived.ejs
<h1><%= title %></h1>
<ul class="message-list">
  <% if (messages.length === 0) { %>
    <li>No archived messages.</li>
  <% } else { %>
    <% messages.forEach(msg => { %>
      <li>
        <strong><a href="/messages/view/<%= msg.id %>"><%= msg.subject %></a></strong><br>
        From: <%= msg.sender_name %> - <%= msg.created_at.toLocaleString() %>
      </li>
    <% }) %>
  <% } %>
</ul>